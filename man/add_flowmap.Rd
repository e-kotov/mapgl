% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flowmap.R
\name{add_flowmap}
\alias{add_flowmap}
\title{Add a flowmap layer to visualize origin-destination flows}
\usage{
add_flowmap(
  map,
  id,
  locations,
  flows,
  color_scheme = "Teal",
  dark_mode = TRUE,
  animation_enabled = FALSE,
  fade_enabled = TRUE,
  fade_amount = 50,
  fade_opacity_enabled = FALSE,
  locations_enabled = TRUE,
  location_totals_enabled = TRUE,
  location_labels_enabled = FALSE,
  clustering_enabled = TRUE,
  clustering_auto = TRUE,
  clustering_level = NULL,
  adaptive_scales_enabled = TRUE,
  highlight_color = "orange",
  max_top_flows = 5000,
  popup = NULL,
  tooltip = NULL
)
}
\arguments{
\item{map}{A map object created by the \code{mapboxgl()} or \code{maplibre()} functions.}

\item{id}{A unique ID for the flowmap layer.}

\item{locations}{A data.frame or sf object containing location data. Must have columns:
\itemize{
\item id: unique location identifier
\item lat, lon: coordinates (or use sf geometry)
\item name: optional location name for labels
}}

\item{flows}{A data.frame containing flow data. Must have columns:
\itemize{
\item origin: origin location id (must match locations$id)
\item dest: destination location id (must match locations$id)
\item count: flow magnitude/weight
}}

\item{color_scheme}{Color scheme for flows. Can be a preset name (e.g., "Teal", "Purple",
"Blue") or a vector of color hex codes. Default is "Teal".}

\item{dark_mode}{Whether to use dark mode styling. Default is TRUE.}

\item{animation_enabled}{Whether to animate flow lines. Default is FALSE.}

\item{fade_enabled}{Whether to fade flow lines by magnitude. Default is TRUE.}

\item{fade_amount}{Fade amount (0-100). Default is 50.}

\item{fade_opacity_enabled}{Whether fade affects opacity. Default is FALSE.}

\item{locations_enabled}{Whether to show location circles. Default is TRUE.}

\item{location_totals_enabled}{Whether location circles scale by totals. Default is TRUE.}

\item{location_labels_enabled}{Whether to show location labels. Default is FALSE.}

\item{clustering_enabled}{Whether to enable location clustering. Default is TRUE.}

\item{clustering_auto}{Whether to auto-adjust clustering level. Default is TRUE.}

\item{clustering_level}{Manual clustering zoom level (0-20). Only used if clustering_auto is FALSE.}

\item{adaptive_scales_enabled}{Whether to use adaptive scaling. Default is TRUE.}

\item{highlight_color}{Color for highlighting on hover. Default is "orange".}

\item{max_top_flows}{Maximum number of top flows to display. Default is 5000.}

\item{popup}{A column name from locations or flows to display in a popup on click.}

\item{tooltip}{A column name from locations or flows to display in a tooltip on hover.}
}
\value{
The modified map object with the flowmap layer added.
}
\description{
Creates an interactive flowmap visualization using deck.gl's FlowmapLayer.
Flowmaps show movement between locations using curved lines with thickness
proportional to flow magnitude. Includes automatic clustering, animation,
and adaptive scaling.
}
\examples{
\dontrun{
library(mapgl)

# Create sample location data
locations <- data.frame(
  id = c("NYC", "LA", "CHI", "HOU", "PHX"),
  name = c("New York", "Los Angeles", "Chicago", "Houston", "Phoenix"),
  lat = c(40.7128, 34.0522, 41.8781, 29.7604, 33.4484),
  lon = c(-74.0060, -118.2437, -87.6298, -95.3698, -112.0740)
)

# Create sample flow data
flows <- data.frame(
  origin = c("NYC", "LA", "CHI", "NYC", "LA"),
  dest = c("LA", "CHI", "HOU", "PHX", "NYC"),
  count = c(1000, 750, 500, 300, 1200)
)

# Create a map with flowmap layer
maplibre(
  center = c(-95, 37),
  zoom = 3
) \%>\%
  add_flowmap(
    id = "flows",
    locations = locations,
    flows = flows,
    color_scheme = "Teal",
    animation_enabled = FALSE
  )
}
}
